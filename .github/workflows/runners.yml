name: Multi-OS runners
on:
    push:
        branches: [main]

jobs:
    ubuntu-job:
        runs-on: ubuntu-latest

        steps:
            - name: prints os name
              run: echo "The OS is ${{ runner.os }}"
